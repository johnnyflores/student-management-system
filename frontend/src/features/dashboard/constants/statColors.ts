import type { StatCardColor } from '@/features/dashboard/types/dashboard';

export const statColors: Record<StatCardColor, string> = {
  blue: 'var(--chart-1)',
  green: 'var(--chart-2)',
  orange: 'var(--chart-3)',
  purple: 'var(--chart-4)',
};
