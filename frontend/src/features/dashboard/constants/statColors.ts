import type { ColorName } from '@/constants/colorStyles';

export const statColors: Record<ColorName, string> = {
  blue: 'var(--chart-1)',
  green: 'var(--chart-2)',
  orange: 'var(--chart-3)',
  purple: 'var(--chart-4)',
};
